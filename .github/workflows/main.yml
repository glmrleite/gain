name: CI
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Run a multi-line script
        run: |
          docker pull honeygain/honeygain
          docker run honeygain/honeygain -tou-accept -email $EMAIL -pass $PASSWD -device github-actions
